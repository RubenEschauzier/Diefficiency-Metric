{
  "name": "diefficiency",
  "version": "1.0.3",
  "main": "src/index.js",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-watch": "tsc -w",
    "build": "tsc",
    "prepare": "npm run build",
    "build-watch:ts": "tsc --watch"
  },
  "dependencies": {
    "fs": "^0.0.1-security",
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "@types/node": "^22.8.7"
  }
}
