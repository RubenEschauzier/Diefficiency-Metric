{
  "name": "diefficiency",
  "version": "1.0.4",
  "main": "src/index.js",
  "license": "MIT",
  "homepage": "https://github.com/RubenEschauzier/Diefficiency-Metric",
  "description": "An implementation of the diefficiency metrics introduced in \"Diefficiency Metrics: Measuring the Continuous Efficiency of Query Processing Approaches\"",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-watch": "tsc -w",
    "build": "tsc",
    "prepare": "npm run build",
    "build-watch:ts": "tsc --watch",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "fs": "^0.0.1-security",
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "@types/node": "^22.8.7"
  }
}
